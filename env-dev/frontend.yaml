env: dev

replicas: 3

tag: latest


secrets:
  required: true
  env:
    catalogue: 10.105.150.24
    user: localhost
    cart: localhost
    shipping: localhost
    payment: localhost

service:
  port: 80
  type: NodePort
  containerPort: 80