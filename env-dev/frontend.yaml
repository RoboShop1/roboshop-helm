env: dev

replicass: chaitu


secrets:
  required: true
  env:
    catalogue: localhost
    user: localhost
    cart: localhost
    shipping: localhost
    payment: localhost

service:
  type: NodePort
  containerPort: 80